{
  "name": "012s-jelly-world",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "validate:assets": "node --experimental-strip-types scripts/validate-pixel-assets.ts",
    "validate:art-v2": "node --experimental-strip-types scripts/validate-v2-art.ts",
    "validate:terrain": "node --experimental-strip-types scripts/validate-pixel-assets.ts --kind terrain",
    "validate:character": "node --experimental-strip-types scripts/validate-pixel-assets.ts --kind character",
    "build:terrain": "node --experimental-strip-types scripts/pixel-assets/build-formal-terrain.ts",
    "build:player-jelly": "node --experimental-strip-types scripts/pixel-assets/build-formal-player-jelly.ts",
    "test:assets": "node --experimental-strip-types scripts/pixel-assets/test-assets.ts"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.7.2",
    "vite": "^6.1.0"
  }
}
